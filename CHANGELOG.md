**버전 규칙**

기준 버전은 0.0이다. 버그 수정 릴리스는 +0.01, 간단한 기능 추가 릴리스는 +0.1, 신규 기능 또는 모델 추가 릴리스는 +1.0씩 증가한다. 릴리스 하나에 포함된 개별 수정 개수로 버전을 반복 증가시키지 않는다. 버전의 단일 원본은 `gui/core/version.py`다.

**5.89 — ONNX 고정밀 경로의 속도 저하 수정**

- 앱 자동 실행에서 느린 전체 FP64 ONNX를 선택하지 않는다. FP32 ONNX 검증 실패 시
  PyTorch로 복귀하며 실패 원인을 표시한다. 명시적 ONNX의 전체 FP64 실행은 속도 경고를 표시한다.
- FP64 Conv의 1×1 연산에서 unfold를 제거하고 depthwise 연산의 Gather·축약을 단순화했다.
- 내보내기에서는 검증된 고정밀 모델을 보존한 상태로 일부 stage를 FP32로 되돌려 검사한다.
  원본 출력 비교를 통과하고 현재 PC에서 모델 추론 중앙값이 5% 이상 개선된 후보만 선택한다.
  스레드 수, 정밀도 구간, 모델 추론 측정값을 저장하고 완료 화면에 표시한다.
- 탐색은 최대 12개 후보와 후보 사이에서 확인하는 30초 제한을 적용한다. 최종 파일을 다시
  읽어 원본과 비교하며 실패한 후보나 느린 후보는 저장하지 않는다. 검증 허용 오차는 그대로다.

**5.88 — EfficientNet ONNX 고정밀 대체 경로와 연산별 진단**

- 기존 FP32 및 BN 변환이 모두 실패하면 내부 FP64 계산을 사용하는 표준 ONNX 그래프를
  검증한다. 입력·출력은 FP32이며 원본 체크포인트와 기존 허용 오차·분류 결과 기준을 유지한다.
  ORT가 지원하지 않는 FP64 Conv/GlobalAveragePool은 MatMul/ReduceMean으로 변환한다.
  이 경로는 속도 최적화가 아니며 추론 시간과 메모리가 증가할 수 있다.
- B1·100×100의 zero/seeded/dynamic batch 검사, 그룹·depthwise Conv, 배포 설정 재로딩을 추가했다.
  사용자 비공개 체크포인트의 직접 실행 결과를 뜻하지 않는다.
- 실패 시 마지막 재시도만 표시하던 문제를 수정하고 상세 JSON 경로를 오류에 표시한다.
  최초 실패 구간의 연산을 동일 입력으로 따로 비교하여 누적 오차와 단일 연산 차이를 구분한다.

**5.87 — EfficientNet ONNX 정규화 계수 보존과 검증 기준 수정**

- 기존 재시도가 실패하면 PyTorch의 BatchNorm 평가 계수를 보존하는 Mul/Add 그래프를 검증한다.
  Conv 융합 및 ORT 최적화를 끄고 검증된 스레드 수를 배포 JSON에 기록한다.
  CPU의 FMA 반올림 차이는 정규화 연산만 DOUBLE로 계산 후 FLOAT로 복귀하는 후보로 별도 검증한다.
- 모든 EfficientNet 후보를 동일한 원본 체크포인트 출력과 비교한다. 후보 자체와의 비교만으로
  Conv/BN 융합 시 생긴 오차가 가려지던 문제를 수정했다. 허용 오차와 top-1 검사는 유지한다.
- 기본 초기화 모델 외에 작은 running variance와 큰 running mean을 가진 BatchNorm의 실제 ONNX
  실행, 원본 기준 변조 검출, 새 재시도 및 배포 재로딩 회귀 테스트를 추가했다.

**5.86 — 사내 Windows 인증서의 LibreYOLO 가중치 다운로드**

- LibreYOLO·Re-DETR v4 기본 가중치를 받을 때도 Windows 시스템 인증서를 적용한다. 사내 HTTPS 검사
  프록시의 root CA가 Windows에 설치되어 있으면 Hugging Face CDN 다운로드가 인증 검증을 유지한 채 동작한다.

**5.85 — 간단 Windows 설치파일**

- `gui\\build.bat installer`가 실행 프로그램과 C++17/C# ONNX 예제만 담은 WiX Setup을 만든다.
- 설치 payload는 `app/`, `Examples/`, `THIRD_PARTY_NOTICES.md`만 허용하도록 검증한다. 사용자 학습
  데이터, Docker/WSL, 저장소 전체는 설치물에 포함되지 않는다. `app/`에는 실행용 기본 자산이 포함된다.
- 설치된 C++/C# 예제는 필요한 SDK 소스를 `vision-runtime/`에 함께 포함해, 저장소 checkout 없이 빌드할 수 있다.

**5.65 — SAM2 Windows native prompt-mask 학습**

- SAM2 Hiera Tiny/Small/Base+/Large가 더 이상 학습 화면에서 차단되지 않는다. 기본 제공 가중치로 prompt encoder와 mask decoder를 미세조정한다.
- semantic class-index mask의 전경 클래스를 이진 객체 마스크와 양성 point prompt로 변환한다. Hiera image encoder는 고정해 GPU 메모리 사용량을 줄인다.
- `best.pt`/`last.pt`는 변경된 SAM2 모듈만 저장한다. 같은 Hiera 기본 가중치와 결합해 재학습과 ONNX encoder/decoder export에 사용하며, 다른 변형 체크포인트는 거부한다.

**5.75 — Best 가중치 이름에 검증 기준 기록**

- Custom CSP, EfficientNet, 기본 제공 ResNet/ConvNeXt/DeepLab/U-Net, LibreYOLO, Re-DETR v4, SAM2, PatchCore가 최종 Best를 `best_<metric>_<value>_epoch_<n>.pt` 형식으로 저장한다.
- 결과 표, 자동 추론 선택, ONNX Export는 새 이름의 실제 Best 파일을 사용한다. 학습 중 임시 `best.pt`는 최종 평가 후 제거하고 `last.pt`는 재개용으로 유지한다.

**3.65 — PatchCore 보정·ONNX 배포 계약 수정**

- 미보정 PatchCore 모델은 C++ ONNX 배포를 시작하지 않도록 막았다. 정상·불량 보정 데이터로 만든 양수 임계값이 있어야 내보낼 수 있다.
- 새 PatchCore는 표준 최근접 패치 점수와 이미지 재가중을 사용한다. 이전 체크포인트는 이전 점수 정의를 보존한다.
- ONNX JSON에 중앙 크롭과 8/16비트 입력 범위를 포함하고, C++ 파일 추론은 uint16 이미지를 전체 범위로 처리한다.
- 정상만 있는 val이 정상·불량 test를 가리지 않도록 보정 split을 선택하고, 보정 불가 시 UI에 ‘판정 미보정’을 표시한다.
- SAM2 Hiera 변형 변경 시 발생하던 Qt 슬롯 예외를 수정했다.

**3.64 — SAM2 가중치 프로젝트 적용과 학습 화면 검증 수정**

- Settings에서 내려받은 SAM2.1 Hiera Tiny/Small/Base+/Large 가중치를 현재 분할 프로젝트에 적용하는 버튼을 추가했다. 선택한 모델 ID와 절대 가중치 경로를 프로젝트에 저장하고, export 화면도 즉시 갱신한다.
- 학습 화면 검증 도구가 EfficientNet에서 제공하지 않는 `custom` 모드를 선택해 CI 전체 회귀 검증을 중단하던 오류를 `efficientnet_scratch` 검사로 바로잡았다.

**3.54 — OBB 저장 실패 검증 회귀 수정**

- OBB 저장 실패를 재현하는 브라우저 검증이 페이지 배너와 편집기 오류를 동시에 선택해 strict locator 오류로 중단되던 문제를 수정했다.

**3.53 — 저장한 지우개 라벨 목록 회귀 수정**

- 브라우저 티칭 검증이 저장된 erase stroke를 foreground 객체처럼 다시 세던 오류를 고쳤다. 저장 문서에는 erase stroke가 남고, 다시 연 객체 목록은 편집 가능한 foreground 두 개만 표시한다.

**3.52 — 라벨 지우기·작은 화면과 SAM2 프롬프트 ONNX 검증 수정**

- 분할 지우개가 객체 목록에 새 라벨처럼 나타나는 브라우저 회귀 검증을 실제 동작에 맞게 고쳤다. 지우개는 배경 subtraction으로 저장되어 다시 열기와 실행 취소에는 남고, 편집할 foreground 객체에는 추가되지 않는다. Shift+브러시와 `[`·`]` 브러시 크기 단축키도 지원한다.
- 검출 편집기는 900 px 폭에서도 창을 강제로 넓히지 않도록 도구 모음을 줄바꿈 그리드로 배치했다.
- SAM2 ONNX 검증은 빈 프롬프트, 양성·음성 점, box의 2/3 라벨, 혼합 점, 이전 mask refinement를 각각 PyTorch 원본과 ONNX Runtime으로 비교한다.

**3.51 — SAM2.1 사전학습·실모델 ONNX 및 라벨 편집 개선**

- Settings에서 공식 SAM2.1 Hiera Tiny/Small/Base+/Large 가중치를 선택해 백그라운드로 내려받는다. Windows 설치본은 고정한 공식 SAM-2 runtime을 포함한다.
- 실제 공식 Hiera Tiny checkpoint의 encoder/prompt decoder를 두 ONNX 그래프로 export하고, 당시 1·2·3·8개 point 입력에서 ONNX Runtime 출력 비교를 통과했다. C++ 실모델 검증과 나머지 변형 실기 검증은 아직 완료로 표시하지 않는다.
- 분할 라벨 편집기에서 Shift+브러시 임시 지우개와 `[`, `]` 크기 단축키를 추가했다.

**2.51 — LibreYOLO ONNX 내보내기 수정**

- LibreYOLO MobileNetV4 Small, LibreYOLO9 Tiny, Re-DETR v4 학습 체크포인트가 ONNX 내보내기에서 미지원 포맷으로 거부되던 오류를 수정했다.
- 공개 LibreYOLO exporter로 그래프를 생성하고, seeded·zero 입력에서 native PyTorch 원본 출력과 ONNX Runtime 출력을 비교한다. C++ SDK는 LibreYOLO 분류·YOLO9·Re-DETR v4 출력 계약을 읽는다.

**2.50 — Settings 모델 카탈로그 정리**

- Settings의 태스크를 Classification, Object Detection, Segmentation, Anomaly Detection으로 통일했다.
- 행 배경 강조와 내부 개발 상태 열을 제거했다. 기본 모델과 추가 Docker 모델의 구분, 태스크, 모델, 실행 방식만 표시한다.

**2.40 — LibreYOLO `.pt` 추론 로드 수정**

- 기본 제공 LibreYOLO9·Re-DETR v4·LibreMobileNetV4 체크포인트를 기존 DVS 전용 형식으로 오인해 거부하던 오류를 수정했다. 앱이 제공하는 공개 `LibreYOLO(path, device=...)` factory로 원본 `.pt`를 복원한다.
- native 분류 확률과 검출 정규화 박스를 기존 추론 결과 화면 형식으로 변환한다. 모델 검사 화면은 native PyTorch 런타임을 표시하고, 지원하지 않는 Grad-CAM 선택을 막는다.

**2.39 — native 학습 대시보드 이벤트 수정**

- LibreYOLO/RT-DETRv4 native 학습이 시작 즉시 진행률과 준비 로그를 표시하고, 매 에폭의 train loss·validation loss·선택 지표·학습률·Best·에폭/누적 시간을 대시보드에 전달한다.
- native 전이·재개 모드의 가중치 경로를 프로젝트를 다시 열어도 학습 화면에 복원한다.

**2.38 — 기본 제공 native 모델 worker**

- LibreMobileNetV4 Small, LibreYOLO9 Tiny, Re-DETR v4 Small/Medium/Large의 native 학습 worker를 연결했다. 모델 선택 후 사전학습·전이·재개·스크래치 초기화를 선택할 수 있다.
- 기존 검출 데이터셋을 upstream YOLO descriptor로 변환하고, epoch 지표와 시간을 앱 실행 기록으로 수집한다.
- Re-DETR v4의 세 크기는 upstream 640×640 고정 입력 계약으로 통일했다.

**1.38 — 모델 선택·ONNX 내보내기·라벨 삭제 수정**

- EfficientNet B0/B1과 ResNet·ConvNeXt·DeepLab V3+·U-Net의 스크래치 학습을 전용 모드로 분리했다. 선택한 모델이 Custom CSP로 바뀌지 않는다.
- 프로젝트를 바꿀 때 이전 프로젝트의 ONNX 체크포인트를 비우고, 현재 프로젝트의 태스크·모델과 충돌하는 체크포인트 내보내기를 시작 전에 차단한다.
- 분할 지우개는 객체 목록에 새 브러시를 추가하지 않고 삭제 작업으로 저장한다. 다시 열기·원본 도형 클래스 변경 뒤에도 지운 구멍을 보존하며, 빈 배경 지우기는 기록하지 않는다.
- 검출 화면에 `E` 객체 클릭 삭제를 추가했다.
- 외부 `.dvmodel` 팩이 기본 제공 모델 ID를 덮어쓰지 못하도록 막았다.

**1.07 — 데이터 티칭 편집기와 Best 기준 수정**

- 신규 분할 마스크 편집기(+1.0): Qt/브라우저에서 다각형, 브러시, 지우개, 꼭짓점 수정, 실행 취소, 저장 후 연속 편집을 지원한다.
- 검출 편집기의 박스 이동·크기 변경·복제·확대·이동과 단축키를 정리했다.
- EfficientNet 외 분류·분할·검출에도 검증 손실을 선택할 수 있다. 기본 모델의 Best 저장·조기 종료·학습 기록에 선택 지표를 적용한다.
- LibreYOLO 완성 팩 미제공 상태와 `.dvmodel` 구성·설치 절차를 명시했다.

**0.07 — 모델별 사전학습 연결 및 SDK 실행 예제**

- 다른 모델에도 EfficientNet 학습 모드가 표시되던 오류와 EfficientNet B0/B1 선택 불일치를 수정했다.
- 누락된 ResNet 18/50, ConvNeXt V1 Tiny의 ImageNet 초기화와 DeepLab V3+/U-Net의 ImageNet 백본 로딩을 연결했다. 로컬 가중치와 가중치 출처 기록도 지원한다.
- `example/`에 C++17/C# ONNX 로딩·반복 추론·출력 검증 예제와 Windows 실행 안내를 추가했다.

**0.04 — EfficientNet ONNX 원본 그래프 자동 재시도**

- 최적화 그래프 검증 실패 시 원본 FP32 그래프를 다시 내보내고 같은 기준으로 검증한다. 두 경로 모두 실패하면 기존 파일을 보존한다.

**0.03 — 레이어 관찰 안내와 모델 카탈로그 표시 수정**

- 내부 개발 단계 표시를 모델 선택 목록에서 제거하고 레이어 관찰의 지원 여부와 기록 상태를 명시했다.

**0.02 — Windows CP949 로그 인코딩 오류 수정**

- 데이터 로딩 상태 메시지의 폴더 이모지가 CP949 출력에서 예외를 일으키던 오류를 수정했다.
- 앱과 공통 학습 엔진에서 콘솔이 표현하지 못하는 문자만 이스케이프하여 학습 중단을 방지한다.

**0.01 — 빈 검증 폴더 학습 분할 수정**

- 새 분류 프로젝트의 빈 `val` 클래스 폴더는 명시적 검증 세트가 아닌 것으로 처리하고, `train` 이미지를 설정한 비율로 학습·검증에 자동 분할한다.
- 데이터 로더가 실패하면 학습 오류에 실제 데이터 경로·형식 원인을 함께 표시한다.

**17.0 — OBB 프로젝트와 편집 화면 안정화**

- OBB 프로젝트 선택 시 필요한 클래스 입력 안내를 표시.
- 여러 회전 박스 생성, 삭제, 클래스 변경과 확대 스크롤을 실제 Qt 이벤트로 검증.
- 미완성 박스 저장 차단과 저장 실패 후 같은 화면에서 재시도하는 흐름을 검증.

**15.9 — 데이터셋 원본 확대와 태스크별 추론 결과**

- 데이터셋 격자의 썸네일을 더블 클릭하면 원본 이미지 확대 화면을 엽니다.
- 데이터셋과 추론 격자에 세로와 가로 스크롤바를 고정하고 격자 안 확대 조작을 제거했습니다.
- 추론 상세 영역을 태스크별로 구성해 분류 전체 확률, 검출 목록, 세그멘테이션 픽셀 분포, 이상 점수와 판정을 표시합니다.

**14.9 — 추론 JSON 크롭과 원본 전체 선택**

- 모든 태스크의 추론에서 모델에 저장된 입력 영역, JSON의 중앙 크롭, 원본 전체 선택 추가.
- JSON 설정을 작업 시작 시 고정하고 사용한 영역과 파일 SHA-256을 결과와 작업 기록에 저장.
- 모델 입력 크기는 유지하며 선택한 영역에 맞게 박스, 마스크와 히트맵 좌표 복원.
- 학습 체크포인트를 수정하지 않고 모델 설정으로 복귀. 잘못된 JSON은 시작 전에 거부.
- 상세 사용법: docs/INFERENCE_REGION.md. 배포 첨부는 소스 ZIP만 제공.

**6.9 — 작은 검출 박스 평가와 추론 상태 오류 수정**

- 정규화 좌표의 작은 박스에서 고정 분모가 IoU를 낮추던 오류 수정. 2048 이미지의 1픽셀 박스도 예측과 정답이 일치하면 IoU 1 유지.
- CPU 평가/측정과 GUI가 같은 체크포인트 입력 크기를 복원. native 모델 내보내기에도 적용하고 실제 입력 크기를 JSON에 기록.
- 학습 설정 키 없이 모델 객체만 포함한 native 체크포인트 인식 수정. 고정 입력 엔진에서 무시되던 크기 변경 옵션은 명시적으로 거부.
- 배치 추론 중 선택한 대기 이미지가 다른 완료 이미지로 바뀌던 오류 수정.
- 미리보기 디스크 저장 실패 시 계산이 끝난 판정과 시간을 보존하고 배치 중단. 이미지별 오류 수와 부분 실패를 완료 요약에 표시.
- 공통 평가기 식별자를 studio-comparison-101-v2로 변경. 과거 작은 객체 mAP는 같은 가중치로 재평가 필요.

**6.8 — 공통 평가 매칭과 CPU 측정 설정 수정**

- 공통 검출 평가에서 이미 매칭된 정답을 제외하고 다음 정답을 탐색하여 겹친 객체의 Recall 과소 계산 수정.
- 공식 추론 초기화가 변경한 PyTorch 스레드 수를 워밍업 후 복원하고 ONNX 세션의 실제 스레드 설정 기록.
- CPU 측정의 워밍업 최소 1회와 공통 평가의 지원 태스크를 실행 전에 검증.

**6.7 — Best 선정 설정, 추론 워커와 CPU 비교 도구**

기능 추가 릴리스로 5.7에서 1.0 증가. 동반 버그 수정은 이 릴리스에 포함한다.

- 공식 모델의 실제 백본 범위로 freeze 지정. 해제 시 빈 목록을 전달하여 resume의 이전 설정도 덮어쓴다.
- Best 기준을 프로젝트에 저장하고 분류 Macro F1/Recall, 검출 및 마스크 지표 선택 지원. 공식 엔진의 저장과 조기 종료에 같은 점수 전달.
- 선정 공식과 실제 점수를 학습 UI, 로그 및 Best 메타데이터에 표시. resume에서 서로 다른 선정 점수의 비교를 차단.
- 모델 계산을 Qt 없는 추론 엔진으로 분리. GUI 시작 버튼은 QThread 워커를 사용하고 진행률, 중단, 모델 교체 방지와 결과 캐시 유지.
- 자체 모델 표시를 Custom CSP로 변경. 기존 CustomCSP Python 클래스명과 체크포인트 키는 호환 유지.
- 분류와 박스 검출을 동일 평가기로 비교하는 CLI 추가. 검출 공통 평가는 101점 보간을 사용하며 이전 VOC11 학습 기록과 명시적으로 구분.
- CPU 측정 CLI에 준비 시간, 워밍업 제외, p50/p95, 추론/Grad-CAM 분리, 환경 정보 및 CSV/JSON 추가.
- Windows/Linux 회귀 검사 후 새 버전 태그를 생성하는 CI 릴리스 작업 추가.

**5.7 — 검출/분할 학습과 결과 표시 수정**

- 검출 박스와 이전 모델 분할 폴리곤의 클래스/좌표/형식 및 train/val 유효 객체를 학습 시작 전에 검증.
- 커스텀 분할 이미지/마스크의 누락, 이름 중복과 원본 크기 불일치를 파일명과 함께 보고.
- 커스텀 검출 좌표를 그리드 기반 정규화 중심과 양수 크기로 계산. 중복 정답 배정을 제거하고 단일 클래스도 BCE로 학습.
- 검출 데이터 증강의 수평 뒤집기 설정을 적용하고 객체 50개 초과를 조용히 잘라내던 처리 제거.
- 클래스 점수와 objectness의 곱, 클래스별 NMS를 평가와 화면 추론에 공통 적용.
- 커스텀 검출 박스와 실제 분할 마스크를 표시하고 원본 해상도 기준 면적 계산. 상세/격자 캐시 유지.
- 홀수/직사각형 입력의 커스텀 분할 출력 크기를 입력과 일치하도록 수정.
- 새 검출 좌표 형식을 체크포인트/ONNX JSON에 기록. 구형 가중치 추론은 기존 좌표식 유지, 새 학습에 전이할 때 백본만 사용.
- 실제 native 검출/분할 학습과 Best CSV, Qt 추론/캐시 및 두 엔진 ONNX 검증 추가.

**5.6 — Grad-CAM 0% 표시와 입력 제외 영역 구분 수정**

- 표시 시작 0%에서도 0값을 강제로 숨기던 조건 수정. 계산된 0값은 최저 강도인 파란색으로 표시.
- 중앙 잘림으로 모델이 보지 않은 원본 영역을 별도 마스크로 보존하고, 반응 0과 구분.
- 유효 영역 마스크를 상세/격자 캐시에 함께 저장하여 이미지 재선택 시에도 같은 표시 유지.
- 입력에 포함된 영역 비율과 바깥이 원본으로 남는 이유를 화면에 표시.
- 0값, 중앙 잘림, 패딩, 캐시 및 실제 Qt/native 분류 경로의 회귀 검사 보완.

**5.5 — PatchCore 사전학습 전이와 안정성 개선**

- ImageNet 백본 3종, 로컬 ResNet 가중치 전이, 기존 PatchCore 재구축/정상 특징 추가 기능.
- 학습 UI에서 가중치 출처, 파일, 특징 한도와 시드를 선택하고 프로젝트에 저장.
- 빈 val/test 폴더 허용, 정상 클래스 선택과 평가 라벨 검증, 파일별 오류 메시지 제공.
- 고해상도 배치, 스트리밍 후보, 투영 코어셋과 양방향 kNN 분할로 메모리 사용 제한.
- 16비트 회색조의 전체 범위와 학습/추론 전처리 일치 보장. 기존 모델은 저장 당시 전처리 유지.
- schema 3 체크포인트에 백본과 출처, 전처리, 구축 정보를 저장하고 원자적 파일 교체 적용.
- 취소/실패 실행 상태와 오류 파일 기록, 적용되지 않는 역전파 설정 비활성화.
- 실제 ResNet 파일 전이, 뱅크 추가, 취소, 저장/복원, Qt 워커와 UI 회귀 검사 추가.
- Windows/Linux CI에 공식 ImageNet 가중치 다운로드 및 학습/추론/오프라인 복원 검사 추가.

**4.5 — 합성 불량 고도화와 Best 결과 내보내기**

- 왼쪽 메뉴 상단을 Deep Vision Studio로 변경.
- Defect Gen에 재현 시드, ROI, 참조 질감, 유형 혼합과 경계/불규칙도 조절 추가.
- 국소 합성과 실제 변경 마스크를 사용하고 16비트 원본 정밀도 보존.
- 생성 마스크와 재현 정보를 별도 주석 ZIP에 저장하며 덮어쓰기와 저장 실패 오집계 방지.
- QThread의 실제 종료 이후 생성 버튼을 다시 활성화하도록 수정.
- CSV에 최종 Best 행과 선정 값 표시, 별도 Best 요약 CSV와 선정 근거 JSON 저장.
- 에폭별 소요/누적 시간과 총 학습 시간을 시:분:초 및 초로 CSV에 저장하고 학습 로그 UI에 표시.
- 실제 선정 지표, 값과 에폭을 포함한 Best 파일명 제공. native 이어학습 CSV 형식 보호.
- 모델별 선정식, 동점 처리, 검증 손실과 PatchCore 예외를 README에 문서화.
- Grad-CAM을 실제 native 추론 모델의 공간 특징과 softmax 전 Linear 점수에 연결.
- 확률만 반환하는 분류 헤드와 고정된 CustomCSP 백본의 Grad-CAM 회귀 검사 추가.
- Qt 이미지의 8비트 RGB 변환과 버퍼 소유권 보장, 실패 상태 문구 보완.

**3.5 — 추론과 Grad-CAM 계산 시간 분리 기능**

- 이미지 결과, 배치 목록과 요약에 추론과 Grad-CAM 계산 시간을 별도로 표시.
- 입력 전처리와 예측 후처리는 추론 시간에, 추가 CAM 전처리와 역전파는 Grad-CAM 시간에 포함.
- CUDA와 MPS는 작업 완료를 동기화하여 비동기 실행의 과소 측정 방지.
- 화면 준비와 캐시 저장 비용은 전체 처리 시간으로 구분.
- 이미지 재선택 시 최초 계산 시간을 유지하고 미실행 또는 실패한 단계는 구분하여 표시.

**2.5 — 베스트 에폭 표시와 추론 결과 재사용, Grad-CAM 수정**

- Train Loss와 Val Loss 카드를 실제 베스트 모델로 선정된 같은 에폭의 값으로 표시.
- 학습 중 베스트 모델이 갱신될 때 Best Epoch와 주요 지표도 함께 갱신.
- 카드에 `(Best)` 표시, Loss 그래프와 로그는 전체 에폭 추이 유지.
- 폐기된 외부 엔진 최종 평가 콜백이 마지막 에폭 손실로 베스트 결과를 덮어쓰는 문제 방지.
- PatchCore처럼 손실을 계산하지 않는 학습 방식은 N/A 표시.
- 이미지 선택 시 재추론을 제거하고 추론 시작 시 계산한 결과와 히트맵을 재사용.
- 이미지별 표시 자료를 임시 파일로 보관하여 전체 원본을 메모리에 누적하지 않음.
- 폐기된 외부 엔진 분류의 확률 대신 원시 logit으로 Grad-CAM을 계산하여 높은 확신도의 기울기 소실 방지.
- 일반 추론과 Grad-CAM의 훅 상태를 분리하고 상수 양수 반응 맵 표시 보존.
- 학습 결과 폴더명에서 마이크로초와 임의 식별자를 제거하고 초 단위까지만 표시. 중복 시에만 짧은 순번 추가.

**2.4 — 클래스 가중치 실제 적용 수정**

- 폐기된 외부 엔진 분류 학습에서 Balanced/Sqrt 설정이 무시되던 연결 누락 수정.
- 커스텀 분류에서 배치 크기 1 또는 같은 클래스만 있는 배치의 가중치 상쇄 방지.
- 실제 학습 subset의 클래스 순서, 표본 수, 적용 가중치를 로그와 실행 기록에 저장.
- 검증 손실은 가중치 없이 계산하여 실험별 비교 기준 유지.
- 큰 가중치의 FP16 변환 오버플로 방지, 기본 폐기된 외부 엔진 형식으로 체크포인트 저장.
- 실제 손실, 역전파, 모델 연결과 저장/복원 회귀 테스트 추가.

**2.3 — Grad-CAM 표현 범위 조절 기능 추가**

- 표시 시작과 최대 강조 값을 0~100% 범위에서 조절하고 기본값 복원.
- 약한 반응은 숨기고 상한 이상은 가장 강한 색상으로 표시, 색상 강도 0~100% 지원.
- 현재 이미지의 반응 맵을 보관하여 재추론 없이 화면만 갱신.
- Grad-CAM 켜기/끄기와 PatchCore 이상 히트맵에 적용, 판정 점수와 임계값 유지.
- 모델/이미지 변경 시 이전 반응 맵 폐기, 실제 수치와 Qt 컨트롤 회귀 테스트 추가.

**1.3 — 추론 이미지 결과 라벨 표시 수정**

- 단일 이미지와 격자에서 공통 위젯으로 좌상단 결과 배지를 표시.
- 추론 결과 변경과 창 크기 변경 시 배지를 다시 그리고 긴 클래스 이름은 말줄임 처리.
- 격자는 최대 512픽셀 축소본을 보관하여 고해상도 배치의 표시 메모리를 제한.
- 원본, Grad-CAM, 이상 히트맵 표시 경로에 동일한 결과 전달.
- 배치 개별 재추론의 목록/격자 갱신과 오류 결과 반영, 보기 전환 시 히트맵 보존.
- 실제 Qt 렌더링과 추론 화면 연결 회귀 테스트 추가.

**1.2 — 원격 검증에서 발견한 실행 환경 수정**

- Linux CI의 Qt 실행에 필요한 EGL/OpenGL 라이브러리 설치.
- 클래스 변경과 학습 코드의 정적 검사 오류 3개 수정.
- 구조 분석 문서에 모듈별 장단점과 후속 분리 순서 보완.

**1.1 — 안전한 클래스 관리 기능 추가**

- 클래스 삭제 전 데이터 영향 범위를 표시하고 백그라운드 작업으로 적용.
- 분류 폴더는 복구용으로 보관하고 탐지/인스턴스 분할 라벨은 클래스 ID를 재정렬.
- 시맨틱 마스크의 배경과 무시값 255 보호, 팔레트와 16비트 값 보존.
- 파일 변경이나 저장 실패 시 데이터와 프로젝트 설정을 함께 복원.
- 클래스 추가/삭제를 즉시 저장하고 학습/추론 페이지 갱신.
- 작업 중 데이터 변경 차단, 이미지와 정답 파일의 이동 충돌 및 실패 복구.
- 구조 분석과 후속 개선 과제를 `docs/ARCHITECTURE.md`에 기록.

**0.1 — 프로젝트와 실행 경로 버그 수정**

- 실제로 열었던 프로젝트 파일에 저장하고, 임시 파일 교체로 저장 실패 시 기존 파일을 보존.
- 프로젝트 내부 경로를 이동 가능한 형식으로 저장하고 이전 파일도 복원.
- 화면 설정 저장 동기화, 입력 크기 복원, 기존 프로젝트 덮어쓰기 방지, 고유 실행 ID.
- 작업 중 프로젝트 전환 보호와 학습 설정 스냅샷.
- 데이터 클래스 매핑, 팔레트 마스크, 채널 전달, 작은 데이터셋과 체크포인트 형식 수정.
- 이상 탐지 로드/평가/판정과 배치 실패 결과 처리 수정.
- GUI/CLI 공통 ONNX 내보내기, 검증 실패 전달, C++ 입력과 태스크 검증.
- Windows frozen 프로세스 진입과 빌드 실패 종료 코드 수정.
- Linux/Windows 회귀 테스트와 C++ 빌드 CI 추가.

**0.0 — 기준 코드**

기준 커밋 `39d2902`에서 `deep-studio-v0.0` 브랜치를 생성.
