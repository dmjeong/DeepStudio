# 0.02: Windows CP949 로그 출력으로 인한 학습 중단 수정

데이터 로더의 폴더 이모지(U+1F4C2)가 Windows CP949 표준 출력에서 UnicodeEncodeError를 발생시켜 학습을 중단하던 오류를 수정했다. 분류·분할·탐지·이상 탐지 로더에서 해당 이모지를 제거했다.

앱 시작 및 공통 학습 엔진 초기화 시 콘솔 인코딩은 유지하면서 표현 불가능한 문자만 이스케이프한다. UTF-8 파일 로그에는 원문을 유지한다. 이전 0.01의 빈 검증 폴더 수정만으로는 이 인코딩 오류가 해결되지 않았다.
