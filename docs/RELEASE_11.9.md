# Deep Vision Studio Desktop 11.9

이전 PatchCore 배치 작업을 다시 열 때 원점수가 표시되던 문제를 수정했습니다.

- 저장된 결과의 PatchCore 식별 정보가 제거되기 전에 0~1 점수로 변환
- 기존 작업 JSON의 원점수와 원래 임계값은 그대로 보존
- 복원된 표, 필터, 슬라이더와 현재 이미지 판정에서 같은 0~1 점수 사용
- 새 추론과 기존 배치 복원 경로를 각각 검증

데스크톱 릴리스에는 소스 ZIP만 첨부합니다.
